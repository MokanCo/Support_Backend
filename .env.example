PORT=5000
MONGODB_URI=mongodb+srv://portal_db_user:4p3XEog7OTS4OzC9@supportportal.oglia5k.mongodb.net/
JWT_SECRET=68f4ecfa22047347db1b4ac77c3f0e9dc41a4732c39ef4f3614c324b9960f253
JWT_EXPIRES_IN=7d
FRONTEND_URL=http://localhost:3000
# On Render, use your HTTPS frontend URL (invite/login links + email logo/backgrounds):
# FRONTEND_URL=https://your-app.onrender.com
# Optional: separate CDN for email images (defaults to FRONTEND_URL)
# EMAIL_ASSETS_URL=https://your-app.onrender.com
# Optional: links in completion emails (falls back to FRONTEND_URL)
# APP_URL=https://your-app.onrender.com
# Optional: OpenAPI server URL in Swagger (defaults to http://localhost:$PORT)
API_PUBLIC_URL=http://localhost:5000

# --- Email (required on Render) ---
# Render blocks outbound SMTP (Gmail/nodemailer ports 587/465). Use Resend over HTTPS:
RESEND_API_KEY=re_xxxxxxxx
RESEND_FROM="Mokanco <noreply@mokanco.com>"
# RESEND_FROM must use a domain you verified at https://resend.com/domains

# Local development only — Gmail via nodemailer (Google App Password):
# SMTP_USER=your@gmail.com
# SMTP_PASS=your-app-password

ADMIN_EMAIL=admin@example.com

# --- Cloudflare R2 (separate buckets for Documents + Marketing Assets) ---
# When set, uploads go to the matching R2 bucket; only the public URL + metadata are saved in MongoDB.
# If unset for a category, that category falls back to local disk under uploads/assets (dev only).
CLOUDFLARE_ACCOUNT_ID=61e546f6674f64aede585eefb0a805d7
CLOUDFLARE_R2_ACCESS_KEY=0c1e36b26e24ffd58df5f59a79c0ce37
CLOUDFLARE_R2_SECRET_KEY=6195f2079edc945438b99515fac7be2a1369f2080de32f80b23bb174b1143067
#
# Documents bucket
CLOUDFLARE_R2_DOCUMENTS_BUCKET_NAME=documents
CLOUDFLARE_R2_DOCUMENTS_PUBLIC_BASE_URL=https://pub-50c18e4d7a1b4f718dc3c779ba79fa63.r2.dev
#
# Marketing assets bucket
CLOUDFLARE_R2_MARKETING_BUCKET_NAME=marketing-assets
CLOUDFLARE_R2_MARKETING_PUBLIC_BASE_URL=https://pub-f9f53a0766c24b3a90721303ed84e275.r2.dev

# Password hashing cost (default 10; use 12 only if you need stronger hashing on fast CPUs)
# BCRYPT_ROUNDS=10
